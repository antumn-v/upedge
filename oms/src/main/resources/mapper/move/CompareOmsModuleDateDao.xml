<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.upedge.oms.modules.move.dao.CompareOmsModuleDateDao">


    <select id="selectNormalOrderNumber"
            resultType="com.upedge.common.model.old.oms.OmsCompareOrderNumberVo">
        select customer_id customerId,pay_state payState,refund_state refundState,ship_state shipState,
        count(1) countNum
            from  `order`
            group by pay_state,refund_state,ship_state ,customer_id
            order by customerId desc
    </select>
    <select id="selectWholesaleOrderNumber"
            resultType="com.upedge.common.model.old.oms.OmsCompareOrderNumberVo">
             select customer_id, pay_state,refund_state,ship_state,count(1) countNum
             from  `wholesale_order`
             group by pay_state,refund_state,ship_state,customer_id
             order by customer_id desc
    </select>
</mapper>