<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.upedge.ums.modules.account.dao.AccountDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.upedge.ums.modules.account.entity.Account" id="BaseResultMap">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="customerId" column="customer_id"/>
        <result property="balance" column="balance"/>
        <result property="rebate" column="rebate"/>
        <result property="credit" column="credit"/>
        <result property="creditLimit" column="credit_limit"/>
        <result property="commission" column="commission"/>
        <result property="status" column="status"/>
        <result property="isDefault" column="is_default"/>
    </resultMap>

    <sql id="Base_Column_List">
        `id`,`name`,`customer_id`,`balance`,`rebate`,`credit`,`credit_limit`,`commission`,`status`,`is_default`
    </sql>

    <select id="selectByPrimaryKey" parameterType="com.upedge.ums.modules.account.entity.Account" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from account
        where id = #{id}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="com.upedge.ums.modules.account.entity.Account">
        delete from account
        where id = #{id}
    </delete>

    <update id="updateByPrimaryKeySelective" parameterType="com.upedge.ums.modules.account.entity.Account">
        update account
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="customerId != null">
                customer_id = #{customerId},
            </if>
            <if test="balance != null">
                balance = #{balance},
            </if>
            <if test="rebate != null">
                rebate = #{rebate},
            </if>
            <if test="credit != null">
                credit = #{credit},
            </if>
            <if test="creditLimit != null">
                credit_limit = #{creditLimit},
            </if>
            <if test="commission != null">
                commission = #{commission},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="isDefault != null">
                is_default = #{isDefault},
            </if>
        </set>
        where id = #{id}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.upedge.ums.modules.account.entity.Account">
        update account
        <set>
        id = #{id},
        name = #{name},
        customer_id = #{customerId},
        balance = #{balance},
        rebate = #{rebate},
        credit = #{credit},
        credit_limit = #{creditLimit},
        commission = #{commission},
        status = #{status},
        is_default = #{isDefault},
        </set>
        where id = #{id}
    </update>

    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="com.upedge.ums.modules.account.entity.Account" useGeneratedKeys="true">
        insert into account
        <trim prefix="(" suffix=")" suffixOverrides=",">
            id,
            name,
            customer_id,
            balance,
            rebate,
            credit,
            credit_limit,
            commission,
            status,
            is_default
        </trim>
        values (
        #{id},
        #{name},
        #{customerId},
        #{balance},
        #{rebate},
        #{credit},
        #{creditLimit},
        #{commission},
        #{status},
        #{isDefault}
        )
    </insert>

    <insert id="insertSelective"  keyColumn="id" keyProperty="id"  parameterType="com.upedge.ums.modules.account.entity.Account" useGeneratedKeys="true">
        insert into account
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null" >
              id
            </if>
            <if test="name != null" >
              name
            </if>
            <if test="customerId != null" >
              customer_id
            </if>
            <if test="balance != null" >
              balance
            </if>
            <if test="rebate != null" >
              rebate
            </if>
            <if test="credit != null" >
              credit
            </if>
            <if test="creditLimit != null" >
              credit_limit
            </if>
            <if test="commission != null" >
              commission
            </if>
            <if test="status != null" >
              status
            </if>
            <if test="isDefault != null" >
              is_default
            </if>

        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null" >
                #{id}
            </if>
            <if test="name != null" >
                #{name}
            </if>
            <if test="customerId != null" >
                #{customerId}
            </if>
            <if test="balance != null" >
                #{balance}
            </if>
            <if test="rebate != null" >
                #{rebate}
            </if>
            <if test="credit != null" >
                #{credit}
            </if>
            <if test="creditLimit != null" >
                #{creditLimit}
            </if>
            <if test="commission != null" >
                #{commission}
            </if>
            <if test="status != null" >
                #{status}
            </if>
            <if test="isDefault != null" >
                #{isDefault}
            </if>

        </trim>
    </insert>

    <insert id="insertByBatch" parameterType="java.util.List">
        insert into account
        <trim prefix="(" suffix=")" suffixOverrides=",">
           id,
           name,
           customer_id,
           balance,
           rebate,
           credit,
           credit_limit,
           commission,
           status,
           is_default
        </trim>
        values
        <foreach collection="list" item="item" index="index" separator=",">
            <trim prefix="(" suffix=")" suffixOverrides=",">
               #{item.id},
               #{item.name},
               #{item.customerId},
               #{item.balance},
               #{item.rebate},
               #{item.credit},
               #{item.creditLimit},
               #{item.commission},
               #{item.status},
               #{item.isDefault}
            </trim>
        </foreach>
    </insert>

    <update id="addAccountBalance">
        update account
        set balance = balance + #{amount}
        where id = #{id}
    </update>

    <sql id="Query_Condition">
        <if test="t != null">
            where
            <trim prefix=" " suffix=" " suffixOverrides=" and ">
                1 = 1 and
                    <if test="t.id != null">
                        id = #{t.id} and
                    </if>
                    <if test="t.name != null">
                        name = #{t.name} and
                    </if>
                    <if test="t.customerId != null">
                        customer_id = #{t.customerId} and
                    </if>
                    <if test="t.balance != null">
                        balance = #{t.balance} and
                    </if>
                    <if test="t.rebate != null">
                        rebate = #{t.rebate} and
                    </if>
                    <if test="t.credit != null">
                        credit = #{t.credit} and
                    </if>
                    <if test="t.creditLimit != null">
                        credit_limit = #{t.creditLimit} and
                    </if>
                    <if test="t.commission != null">
                        commission = #{t.commission} and
                    </if>
                    <if test="t.status != null">
                        status = #{t.status} and
                    </if>
                    <if test="t.isDefault != null">
                        is_default = #{t.isDefault} and
                    </if>
            </trim>
        </if>
    </sql>

    <select id="select" parameterType="com.upedge.common.base.Page" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from account
        <include refid="Query_Condition"/>
        <if test="orderBy != null">
            order by ${orderBy}
        </if>
        <if test="pageNum != null and pageSize != null">
            limit #{fromNum,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER}
        </if>
    </select>

    <select id="count" parameterType="com.upedge.common.base.Page" resultType="java.lang.Long">
        select
        count(1)
        from account
        <include refid="Query_Condition"/>
    </select>


</mapper>